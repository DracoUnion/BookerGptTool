SCHEMA_PMT = '''
是一个严苛的知识架构师。你的任务是分析一本书的目录和前言，推断该书的知识结构。

## 规则

1. domains 使用 kebab-case 英文命名
2. 仅基于目录和前言内容推断，禁止编造
3. 直接输出 JSON，禁止输出任何解释性文字

## 格式

你必须输出一个 JSON 对象，包含以下字段：

```
{
  "book_type": "技术手册 | 方法论 | 操作规范 | 学术教材 | 叙事类",
  "domains": ["领域1", "领域2", ...],
  "core_components": ["该书应该覆盖的核心组件/概念"],
  "skill_types": ["该书能提取的 Skill 类型，如：故障排查、配置部署、分析框架"]
}
```

## 目录

[content]
{toc}
[/content]

## 前言

[content]
{preface}
[/content]
'''

############################################################

TECH_EXT_PMT = '''
你是一个严苛的系统架构师和逆向工程专家。你的任务是从技术文档中提取可执行的操作规范。

## 核心规则

1. 忽略所有背景铺垫、历史故事和情绪化表达
2. 只提取具有前置条件、因果关系、操作步骤的硬核规则
3. 仅允许使用原文中明确出现的值，若原文未提供某个参数，强制填入 null，严禁根据常识推导
4. 直接输出最终数据结构，禁止输出"好的"、"如下所示"等任何过渡性语言
5. 如果原文没有可提取的操作规范，输出 EMPTY_BLOCK
6. 在提取前，先列出原文中所有的条件触发词（如果、当、必须、若）和动作动词，强迫自己扫描全图，避免遗漏关键步骤
7. 每个 Skill 必须包含至少一个 IF/ELSE 判断分支。如果原文没有显式分支，根据操作结果构造"成功/失败"二元分支

## 要求

请从给定文本中提取所有可执行的操作规范，每个规范输出为一个独立的 Skill。
如果文本中包含多个独立的操作规范，输出多个 Skill（用 --- 分隔）。
如果文本中没有可提取的操作规范，输出 EMPTY_BLOCK。

## 待提取的文本块

> {context}

[content]
{content}
[/content]

## 格式

你必须严格按以下 YAML Frontmatter 格式输出每个 Skill：

```yaml
name: <kebab-case 英文名称，全局唯一>
trigger: <中文描述：什么场景下调用此 Skill>
domain: <从以下领域中选择：{domain_list}>
prerequisites:
  - <前置依赖，如"需要获取XX数据">
source_ref: <原文出处：章节名称>
confidence: <0.0-1.0 的置信度自评>
```

Frontmatter 之后是 Markdown 正文，包含：
- 「# 执行步骤」：用 1, 2, 3 编号，包含 IF/ELSE 判断分支
- 「# 输出格式要求」：规定 AI 最终回答用户的格式
'''

############################################################

NARRATIVE_EXT_PMT = '''
你是一个深度叙事分析师。你的任务是从小说/叙事文本中提取结构化的「叙事知识单元」。

每个单元必须包含以下要素（缺失则填 null）：

- **人物**：涉及的关键角色及其身份/立场
- **事件**：发生了什么，核心矛盾/冲突是什么
- **时间线**：事件在故事中的时间位置（章节/阶段）
- **因果链**：为什么会发生？导致了什么后果？
- **决策逻辑**：关键人物做了什么决策？依据是什么？结果如何？
- **主题标签**：该事件属于什么主题（商业决策/人物成长/阴谋博弈/伏笔回收等）

## 核心规则

1. 每个决策或关键事件必须有完整的 IF/THEN 逻辑链
2. 人物的动机和目标必须被明确提取
3. 保留因果关系的完整性：起因 → 行动 → 结果 → 后续影响
4. 如果事件涉及多个人物的博弈，必须分别描述各方视角
5. 如果原文仅为过渡/日常描写，无关键事件，输出 EMPTY_BLOCK
6. 直接输出最终数据结构，禁止输出分析过程

## 要求

请从给定文本中提取所有关键「叙事知识单元」。如果文本中包含多个独立事件，输出多个单元（用 --- 分隔）。
如果文本仅为过渡/日常描写，无关键事件或决策，输出 EMPTY_BLOCK。

## 待提取的文本块（来自叙事作品）

> {context}

[content]
{content}
[/content]

## 格式

每个单元输出为如下 YAML Frontmatter + Markdown Body 格式：

[content]
---
name: <事件的 kebab-case 英文名>
trigger: "<事件的一句话描述>"
domain: <所属主题领域>
characters:
  - "<角色1: 身份/立场>"
  - "<角色2: 身份/立场>"
timeline: "<在故事中的时间位置，如 第X章/第N个投资周期>"
source_ref: "<来源章节>"
confidence: <0.0-1.0>
prompt_version: "v0.1"
---

# 事件概要
<一段话概括发生了什么>

# 人物动机与决策
<关键角色的动机、目标、决策逻辑>

# 因果链
1. 起因：<为什么会发生>
2. 行动：<做了什么>
3. 结果：<导致了什么>
4. 后续影响：<对后续情节的影响>

# 核心逻辑
<IF/THEN 形式的决策逻辑>

# 主题标签
<该事件属于什么主题：商业决策/人物成长/阴谋博弈/伏笔回收等>
[/content]
'''

############################################################

METHOD_EXT_PMT = '''
你是一位方法论逆向工程专家。你的任务是从方法论/框架类文档中提取可复用的「思维模型」。

每个模型必须包含以下要素：

- **模型名称**：该方法论的一句话命名
- **适用场景**：在什么条件下使用这个模型
- **核心步骤**：按顺序排列的操作步骤，每步必须可执行
- **判断标准**：如何判断该步骤是否成功
- **常见误区**：容易犯的错误和反例
- **与其他模型的关系**：前置条件/后续衔接/互斥关系

## 核心规则

1. 每个步骤必须有明确的输入和输出
2. IF/ELSE 分支必须覆盖所有可能的结果
3. 抽象层级要适中：不能太具体（变成操作手册）也不能太抽象（变成哲学）
4. 如果文本仅为背景引入或作者个人经历，输出 EMPTY_BLOCK
5. 直接输出最终数据结构，禁止输出过渡性语言

## 要求

请提取可复用的思维模型或方法论框架，每个输出为一个独立的 Skill（用 --- 分隔）。
无方法论内容则输出 EMPTY_BLOCK。

## 待提取的文本块

> {context}

[content]
{content}
[/content]

## 格式

你必须严格按以下 YAML Frontmatter 格式输出每个 Skill：

```yaml
name: <kebab-case 英文名称，全局唯一>
trigger: <中文描述：什么场景下调用此 Skill>
domain: <从以下领域中选择：{domain_list}>
prerequisites:
  - <前置依赖，如"需要获取XX数据">
source_ref: <原文出处：章节名称>
confidence: <0.0-1.0 的置信度自评>
```

Frontmatter 之后是 Markdown 正文，包含：
- 「# 执行步骤」：用 1, 2, 3 编号，包含 IF/ELSE 判断分支
- 「# 输出格式要求」：规定 AI 最终回答用户的格式
'''

############################################################

ACADEMIC_EXT_PMT = '''
你是一位学术知识提取器。你的任务是从教材/学术文档中提取结构化的「知识图谱节点」。

每个节点必须包含以下要素：

- **概念名称**：核心概念的精确定义
- **前置知识**：理解此概念需要先掌握什么
- **核心原理**：该概念的内在逻辑/数学关系/因果机制
- **应用示例**：具体的使用场景或例题
- **边界条件**：在什么条件下该概念不成立或需要修正
- **关联概念**：与其他概念的层级/对比/组合关系

## 核心规则

1. 定义必须精确到可以作为考试评分标准的程度
2. 公式/定理必须保留完整的数学表达
3. 前置知识链条不能断裂
4. 如果文本仅为练习题或索引，输出 EMPTY_BLOCK
5. 直接输出最终数据结构

## 要求

请提取知识图谱节点：概念定义、前置知识、核心原理、示例。
每个概念输出为一个独立的 Skill（用 --- 分隔）。
纯练习题或索引则输出 EMPTY_BLOCK。

## 待提取的文本块

> {context}

[content]
{content}
[/content]

## 格式

你必须严格按以下 YAML Frontmatter 格式输出每个 Skill：

```yaml
name: <kebab-case 英文名称，全局唯一>
trigger: <中文描述：什么场景下调用此 Skill>
domain: <从以下领域中选择：{domain_list}>
prerequisites:
  - <前置依赖，如"需要获取XX数据">
source_ref: <原文出处：章节名称>
confidence: <0.0-1.0 的置信度自评>
```

Frontmatter 之后是 Markdown 正文，包含：
- 「# 执行步骤」：用 1, 2, 3 编号，包含 IF/ELSE 判断分支
- 「# 输出格式要求」：规定 AI 最终回答用户的格式
'''

############################################################

INSURANCE_EXT_PMT = '''
你是一位资深保险精算师和合规审查专家。你的任务是从保险合同、保单条款和理赔文档中提取结构化的知识规则。

## 核心规则

1. 提取每条保障/免责条款的完整描述（编号 + 条文内容 + 适用范围）
2. 所有金额、比例、期限、免赔额等数字必须精确保留，严禁省略或近似
3. 提取条件分支：IF 条件（年龄/病种/事故类型） THEN 赔付方式/拒赔理由
4. 区分：保障范围 vs 免责条款 vs 理赔条件 vs 等待期规则
5. 如果原文没有可提取的保险规则，输出 EMPTY_BLOCK
6. 直接输出最终数据结构，禁止输出"好的"、"如下所示"等任何过渡性语言

## 要求

请提取所有保障条款、免责条款、理赔规则和赔付标准。
每个条款/规则输出为一个独立的 Skill（用 --- 分隔）。
纯目录或法律声明则输出 EMPTY_BLOCK。

## 待提取的文本块

> {context}

[content]
{content}
[/content]

## 格式

你必须严格按以下 YAML Frontmatter 格式输出每个 Skill：

```yaml
name: <kebab-case 英文名称，全局唯一>
trigger: <中文描述：什么场景下调用此 Skill>
domain: <从以下领域中选择：{domain_list}>
prerequisites:
  - <前置依赖，如"需要获取XX数据">
source_ref: <原文出处：章节名称>
confidence: <0.0-1.0 的置信度自评>
```

Frontmatter 之后是 Markdown 正文，包含：
- 「# 执行步骤」：用 1, 2, 3 编号，包含 IF/ELSE 判断分支
- 「# 输出格式要求」：规定 AI 最终回答用户的格式
'''

############################################################

REPORT_EXT_PMT = '''
你是一位资深行业分析师和数据解读专家。你的任务是从行业报告、研究报告和白皮书中提取结构化的分析框架和关键数据。

## 核心规则

1. 提取关键数据指标（市场规模、增长率、份额、趋势判断），保留精确数字和单位
2. 提取分析框架和评估模型（维度、权重、方法论）
3. 表格和图表中的数据转为结构化键值对
4. 区分：事实性数据 vs 预测性判断 vs 方法论框架
5. 如果原文没有可提取的分析内容，输出 EMPTY_BLOCK
6. 直接输出最终数据结构，禁止输出"好的"、"如下所示"等任何过渡性语言

## 要求

请提取关键数据指标、分析框架和趋势判断。
每个数据点或分析模型输出为一个独立的 Skill（用 --- 分隔）。
纯致谢或免责声明则输出 EMPTY_BLOCK。

## 待提取的文本块

> {context}

[content]
{content}
[/content]

## 格式

你必须严格按以下 YAML Frontmatter 格式输出每个 Skill：

```yaml
name: <kebab-case 英文名称，全局唯一>
trigger: <中文描述：什么场景下调用此 Skill>
domain: <从以下领域中选择：{domain_list}>
prerequisites:
  - <前置依赖，如"需要获取XX数据">
source_ref: <原文出处：章节名称>
confidence: <0.0-1.0 的置信度自评>
```

Frontmatter 之后是 Markdown 正文，包含：
- 「# 执行步骤」：用 1, 2, 3 编号，包含 IF/ELSE 判断分支
- 「# 输出格式要求」：规定 AI 最终回答用户的格式
'''

############################################################

MED_LGL_EXT_PMT = '''
你是一位具备医学和法律双重背景的专业知识提取专家。你的任务是从医学文献、法律法规和金融规则文档中提取精准的专业规则。

核心规则：

1. 提取专业术语的完整定义及其适用语境
2. 所有剂量、阈值、判定标准、时限等关键数字必须精确保留
3. 提取条件分支：IF 症状/条件/情形 THEN 处理方案/法律后果
4. 区分：必须执行项 vs 建议性措施 vs 禁忌项/禁止事项
5. 如果原文没有可提取的专业规则，输出 EMPTY_BLOCK
6. 直接输出最终数据结构，禁止输出"好的"、"如下所示"等任何过渡性语言

## 要求

请提取专业术语定义、判定标准和条件分支规则。
每个术语/标准输出为一个独立的 Skill（用 --- 分隔）。
纯参考文献或索引则输出 EMPTY_BLOCK。

## 待提取的文本块

> {context}

[content]
{content}
[/content]

## 格式

你必须严格按以下 YAML Frontmatter 格式输出每个 Skill：

```yaml
name: <kebab-case 英文名称，全局唯一>
trigger: <中文描述：什么场景下调用此 Skill>
domain: <从以下领域中选择：{domain_list}>
prerequisites:
  - <前置依赖，如"需要获取XX数据">
source_ref: <原文出处：章节名称>
confidence: <0.0-1.0 的置信度自评>
```

Frontmatter 之后是 Markdown 正文，包含：
- 「# 执行步骤」：用 1, 2, 3 编号，包含 IF/ELSE 判断分支
- 「# 输出格式要求」：规定 AI 最终回答用户的格式
'''

############################################################

PROC_EXT_PMT = '''
你是一个严苛的流程工程师。你的任务是从文本中提取**程序型知识单元**。

程序型知识包括但不限于：

- **操作流程**：标准操作步骤、仪式程序、行政流程
- **决策策略**：IF/THEN 决策树、博弈策略、应对方案
- **战术方法**：军事战术、谈判技巧、生存技能
- **工作方法论**：管理方法、组织方式、协调机制
- **诊断/排查**：问题定位流程、故障排除步骤

## 核心规则

1. 每个程序型单元必须有完整的**步骤序列**（Step 1 → Step 2 → ...）
2. 每个步骤必须有**前置条件**和**预期结果**
3. 决策节点必须有 IF/THEN/ELSE 完整分支
4. 保留所有**具体参数**（人数、时间、距离等），严禁泛化
5. 如果某个步骤依赖特定角色或工具，必须明确标注
6. 纯叙事/描写段落若无可提取流程，输出 EMPTY_BLOCK
7. 直接输出最终数据结构，禁止输出分析过程

## 要求

请从给定文本中提取所有可执行的操作规范，每个规范输出为一个独立的 Skill。
如果文本中包含多个独立的操作规范，输出多个 Skill（用 --- 分隔）。
如果文本中没有可提取的操作规范，输出 EMPTY_BLOCK。

## 待提取的文本块

> {context}

[content]
{content}
[/content]

## 格式

你必须严格按以下 YAML Frontmatter 格式输出每个 Skill：

```yaml
name: <kebab-case 英文名称，全局唯一>
trigger: <中文描述：什么场景下调用此 Skill>
domain: <从以下领域中选择：{domain_list}>
prerequisites:
  - <前置依赖，如"需要获取XX数据">
source_ref: <原文出处：章节名称>
confidence: <0.0-1.0 的置信度自评>
```

Frontmatter 之后是 Markdown 正文，包含：
- 「# 执行步骤」：用 1, 2, 3 编号，包含 IF/ELSE 判断分支
- 「# 输出格式要求」：规定 AI 最终回答用户的格式
'''

############################################################

DFT_EXT_PMT = PROC_EXT_PMT

