CLS_FUNC_EXT_PMT = '''
你是一位资深架构师，文档专家和逻辑分析专家。你的工作是遵循一系列的步骤，生成给定代码的源码解析书籍。

## 任务

你的当前任务是根据给定代码，提取其中的类，方法和全局函数。按照给定格式以 JSON 输出。

## 要求

1.  输出包含一段话描述，用一段话概述该代码的核心功能。
2.  分析代码的整体工作流程，输出 mermaid。
3.  分析代码的类层次结构，输出树状图。
2.  识别出代码中的所有类名、类方法名和全局函数名,及其一句话描述。
3.  将这些元素分类，准备进入下一步的详细解析。
4.  JSON 格式输出，注意输出包含在三个反引号（```）中。

## 格式

```
{
    "desc": "the file ....",
    "process": [
        "graph TD",
        "    A[开始] --> B{金额 > 0?}",
        "    B -- 否 --> C[抛出 ValueError]",
        "    B -- 是 --> D[调用第三方支付网关]",
        "    D --> E{网关返回成功?}",
        "    E -- 是 --> F[更新订单状态]",
        "    E -- 否 --> G[记录日志并重试]",
        "    ..."
    ],
    "structure": [
        "ModelBase (抽象基类)",
        "├── TextModel (文本模型基类)",
        "│   ├── LlamaModel",
        "│   ├── GPT2Model",
        "│   ├── FalconModel",
        "│   ├── Qwen2Model",
        "│   ├── GemmaModel",
        "│   └── ... "
    ],
    "classes": [
        {
            "name": "Class1",
            "desc": "the class ...",
            "methods": [
                {
                    "name": "method1",
                    "desc": "the method ..."
                },
                { ... }
            ]
        },
        { ... }
    ],
    "funcs": [
        {
            "name": "func1",
            "desc": "the func ..."
        },
        { ... }
    ]
}
```

## 代码

`{fname}`

```
{code}
```
'''

################################################

OUTLINE_PMT = '''
你是一位资深架构师，文档专家和逻辑分析专家。你的工作是遵循一系列的步骤，生成给定代码的源码解析书籍。

## 任务

你的当前任务是根据给定项目结构和源码描述，编排书籍大纲，大纲分为部分（`parts`）-章（`chapters`）-知识点（`nodes`）三级结构，应包含部分名称，部分描述，章名称，章描述，知识点名称，知识点描述，知识点对应源码文件。按照指定格式以 JSON 输出。

## 要求

1.  章标题采取两段式结构，注重类比，例如：
    +    llama.cpp 概览 —— 认识这座"大模型推理引擎
    +    环境搭建与构建系统 —— 打造你的"AI 开发工作站"
    +    GGML 核心架构 —— 理解"张量世界的乐高积木"
    +    张量运算实现 —— 探索"数值计算的瑞士军刀"
    +    量化技术深度解析 —— 模型压缩的"瘦身魔法"
2.  JSON 格式输出，注意输出包含在三个反引号（```）中。

## 格式

```
{
    "parts": [
        {
            "no": 1,
            "name": "基础入门",
            "desc" : "...",
            "chapters": [
                {
                    "no": 1,
                    "name": "llama.cpp 概览 —— 认识这座"大模型推理引擎",
                    "desc": "本章带你鸟瞰 llama.cpp 的全貌，了解它如何以"零依赖、单文件即可运行"的极简哲学，实现跨平台高性能大模型推理。",
                    "nodes": [
                        {
                            "no": 1,
                            "name": "什么是 llama.cpp",
                            "desc": "用生活类比理解：一个能把庞大AI模型装进你口袋的"压缩魔法盒",
                            "src": ["README.md", "include/llama.h", "..."]
                        }, 
                        { ... }
                    ]
                },
                { ... }
            ]
        }, 
        { ... }
    ]
}
```

## 项目结构

[content]
{struct}
[/content]

## 源码描述

[coontent]
{code_desc}
[/content]

## README
[content]
{readme}
[/content]
'''

################################################

SRC_ANLS_DETAIL_PMT = '''
你是一位资深架构师，文档专家和逻辑分析专家。你的工作是遵循一系列的步骤，生成给定代码的源码解析书籍。

## 任务

你的任务是根据第{i}章的大纲和对应源码，编写细纲的源码解析部分，按照指定格式以 JSON 输出。

## 要求

1.  细纲包含若干个源码解析单元（`unit`），由大纲的知识点（`nodes`）拆分而成，每个单元只包含要点（`points`），以列表格式呈现，以及源码位置，包含文件名（`file`），以及类名或函数名（`class_or_func`）和行数（`lines`）。
2.  单元名称采取两段式结构，注重类比，例如：
    +    llama.cpp 概览 —— 认识这座"大模型推理引擎
    +    环境搭建与构建系统 —— 打造你的"AI 开发工作站"
    +    GGML 核心架构 —— 理解"张量世界的乐高积木"
    +    张量运算实现 —— 探索"数值计算的瑞士军刀"
    +    量化技术深度解析 —— 模型压缩的"瘦身魔法"
3.  JSON 格式输出，注意输出包含在三个反引号（```）中。


## 格式

```
{
    "units": [
        { 
            "no": 1,
            "name": "基础数学运元素级运算 —— 传送带上的并行加工算",
            "points": [
                "**为什么需要 `src[]` 数组？**",
                "- 计算图需要知道每个节点的"原料"来自哪里",
                "- 执行时根据 `op` 类型和 `src` 输入进行计算",
                "- 反向传播时（训练场景）需要追踪梯度来源",
                "..."
            ],
            "code": [
                {
                    "file": "ggml/src/ggml.c",
                    "class_or_func": "ggml_mul_mat()",
                    "line": "15000-15200",
                },
                { ... }
            ]
        },
        { ... }
    ]
}
```

## 大纲

```
{outline}
```

## 对应源码

[content]
{code}
[/content]
'''

################################################

REST_DETAIL_PMT = '''
你是一位资深架构师，文档专家和逻辑分析专家。你的工作是遵循一系列的步骤，生成给定代码的源码解析书籍。

## 任务

你的任务是根据细纲的源码解析部分和对应源码，编写细纲的剩余部分，包括学习目标、源码地图、生活类比、动手练习，以及总结。按照给定格式以 JSON 输出。

## 要求

1.  学习目标仅仅以列表形式呈现，例如：
    1.  理解GGML基础数学运算的实现原理
    2.  掌握神经网络专用算子（SiLU、RMSNorm、RoPE）的源码
    3.  了解内存优化技术（in-place、view、复用）的实现机制
    4.  能阅读并修改自定义算子
2.  生活类比高度概括以列表形式呈现，例如：
    -   想象GGML的计算图是一家**智能工厂的生产流水线**：
        -   **张量** = 传送带上的物料箱
        -   **运算操作** = 加工机器（加法机、乘法机、混合机）
        -   **内存池** = 仓库货架（预先规划好的存储空间）
        -   **in-place优化** = 原地改造（不搬动物料，直接加工）
        -   **view机制** = 贴标签（多个箱子共享同一批货物）
    -   就像工厂经理需要精心安排每台机器的位置和物料流转，GGML的计算图也需要精确规划每个算子的内存使用和计算顺序。
3.  源码地图涵盖细纲涉及到的所有源码文件，树状图形式呈现，展开到类方法和函数层次。
4.  总结包含细纲涉及到的所有概念和一句话总结，例如：
    | 概念 | 一句话总结 |
    |------|-----------|
    | `ggml_add/mul` | 元素级运算，形状必须完全相同 |
    | `ggml_mul_mat` | 矩阵乘法，深度学习核心运算 |
    | `ggml_silu` | Swish激活，LLM首选激活函数 |
    | `ggml_rms_norm` | RMS归一化，LayerNorm的简化版 |
    | `ggml_rope` | 旋转位置编码，注入位置信息 |
    | `ggml_view_*` | 视图操作，零拷贝形状变换 |
    | `ggml_*_inplace` | 原地操作，节省50%内存 |
5.  动手练习仅仅以列表形式呈现，最少生成三个，注意不要给答案，例如：
    -   ### 练习1：阅读GGML算子实现
    -   阅读 `ggml/src/ggml.c` 第17200-17800行，理解以下算子的实现：
        1. `ggml_gelu()` - GELU激活函数
        2. `ggml_norm()` - LayerNorm
        3. `ggml_soft_max()` - Softmax
    -   回答问题：
        - 这三个算子的 `op_params` 各存储了什么参数？
        - 它们的输入输出形状关系是什么？

## 格式

```
{
    "learning_targets": [
        "理解GGML基础数学运算的实现原理",
        "掌握神经网络专用算子（SiLU、RMSNorm、RoPE）的源码",
        "了解内存优化技术（in-place、view、复用）的实现机制",
        "能阅读并修改自定义算子",
        "..."
    ],
    "code_map": [
        "ggml/src/ggml.c",
        "├── 基础运算（15000-16000行）",
        "│   ├── ggml_add()           # 张量加法",
        "│   ├── ggml_add_inplace()   # 原地加法",
        "...",
        "├── 矩阵运算（16000-17000行）",
        "│   ├── ggml_mul_mat()       # 矩阵乘法 GEMM",
        "...",
        "ggml/include/ggml.h",
        "├── enum ggml_op             # 操作类型枚举",
        "│   ├── GGML_OP_ADD/MUL/SUB/DIV"",
        "..."
    ],
    "life_analogy": [
        "想象GGML的计算图是一家**智能工厂的生产流水线**：",
        "- **张量** = 传送带上的物料箱",
        "- **运算操作** = 加工机器（加法机、乘法机、混合机）",
        "- **内存池** = 仓库货架（预先规划好的存储空间）",
        "- **in-place优化** = 原地改造（不搬动物料，直接加工）",
        "- **view机制** = 贴标签（多个箱子共享同一批货物）",
        "就像工厂经理需要精心安排每台机器的位置和物料流转，GGML的计算图也需要精确规划每个算子的内存使用和计算顺序。"
    ],
    "summary": [
        {'concept": "`ggml_add/mul`", "desc": "元素级运算，形状必须完全相同"},
        {'concept": "`ggml_mul_mat`", "desc": "矩阵乘法，深度学习核心运算"},
        {'concept": "`ggml_silu`", "desc": "Swish激活，LLM首选激活函数"},
        {'concept": "`ggml_rms_norm`", "desc": "RMS归一化，LayerNorm的简化版"},
        {'concept": "`ggml_rope`", "desc": "旋转位置编码，注入位置信息"},
        {'concept": "`ggml_view_*`", "desc": "视图操作，零拷贝形状变换"},
        {'concept": "`ggml_*_inplace`", "desc": "原地操作，节省50%内存"},
        { ... }
    ],
    "exercises": [
        {
            "no": 1,
            "title": "阅读GGML算子实现",
            "contents": [
                "阅读 `ggml/src/ggml.c` 第17200-17800行，理解以下算子的实现：",
                "1. `ggml_gelu()` - GELU激活函数",
                "2. `ggml_norm()` - LayerNorm",
                "3. `ggml_soft_max()` - Softmax",
                "回答问题：",
                "- 这三个算子的 `op_params` 各存储了什么参数？",
                "- 它们的输入输出形状关系是什么？"
            ]
        }, 
        { ... }
    ],
}
```

## 细纲的源码解析部分

```
{detail}
```

## 对应源码

[content]
{code}
[/content]
'''

########################################################

BODY_PMT = '''
你将扮演一位资深技术写作者，专门将复杂的源代码转换为深入浅出的技术教程。你的工作是遵循一系列的步骤，生成给定代码的源码解析书籍。

## 任务

你的当前任务是根据给定细纲和模板，参考示例片段，编写第{i}章正文。

## 要求

-   以 Markdown 格式输出
-   学习目标照搬细纲的`learning_targets`部分，不要修改。
-   生活类比需要是完整段落，由细纲的`life_analogy`部分扩写而成
-   源码地图是树状图形式，照搬细纲的`code_map`部分，不要修改
-   动手练习是列表形式，照搬细纲的`exercises`部分，不要修改
-   源码解析单元采用源码驱动，从代码到故事
    -   **起点是源码**：从实际源代码出发，而非从概念出发
    -   **逐行解析**：先给完整代码，代码需要逐行注释，解释每一行的作用。代码块后添加一段话总结代码块作用（这段代码定义/展示/实现了 xxx）。
    -   **类型即图纸**：将TypeScript/Python/Cpp/Golang类型定义视为"设计图纸"先讲解，后跟关键函数详解
    -   **代码与 narrative 交织**：代码块和解释文字比例约为 1:1
-   **语言风格**：
    -   使用"你"或者“我们”直接对话读者
    -   短段落（不超过4行）
    -   多用列表、表格、代码块
    -   每个列表项需要是段落而非句子（学习目标和动手练习除外）。
    -   表格前必须有一句话介绍（以下是 xxx）。
    -   代码块需要逐行注释，解释每一行的作用。代码块后添加一段话总结代码块作用（这段代码定义/展示/实现了 xxx）。
    -   代码块需要标注行号范围，并对比不同解决方案。
-   源码解析单元的代码块**必须标注**：
    -   源码文件路径：`src/xxx/yyy.ts`（第X-Y行）
    -   核心类/接口名：`ClassName`
    -   关键函数：`functionName()`
-   源码解析单元**可以包含如下形式的图表**：
    1.  **模块依赖图**：展示文件之间的关系
    2.  **数据流图**：状态/数据如何流转
    3.  **时序图**：关键调用链
    4.  **架构分层图**：系统层次结构
    使用Mermaid语法：
    ```mermaid
    graph TD
        A[入口] --> B[核心模块]
        B --> C[子模块1]
        B --> D[子模块2]
    ```
-   小结首先包含一段话概述，将细纲中的`summary`扩写而成，然后包含一个表格总结，直接照搬`summary`，然后包含一段话的下一章预告。

## 模板

[content]
# {章标题}

## 学习目标

（列表形式，照搬细纲的`learning_targets`部分，不要修改）

## x.y 生活类比

（完整段落，由细纲的`life_analogy`部分扩写而成）

## x.y 源码地图

（树状图形式，照搬细纲的`code_map`部分，不要修改）

## x.y {源码解析单元1}

（核心概念解释，完整段落，由细纲的`unit[*].points`扩写而成）

（核心类型定义：）

源码路径：`src/xxx/yyy.ts` - `ClassName`（x-y行）

```
// 类型即图纸，先理解数据结构
```

（逐行解析关键函数：）

源码路径：`src/xxx/yyy.ts` - `functionName()`（x-y行）

```
// 逐行注释解释
```

（每个代码块之后都需要有一段话概述代码作用）

这段代码 xxx ...

（完整数据流/流程图）

```mermaid
# 用Mermaid描述流程
```

## x.y {源码解析单元2}

（...）

## x.y 设计中的取舍

（一问一答形式，完整段落不要列表）

- 为什么不用XXX方案？
- 这种设计的trade-off是什么？

## x.y 动手练习

（列表形式，照搬细纲的`exercises`部分，不要修改）

## x.y 本章小结

（将细纲中的`summary`扩写成一段话）

这一章中我们学习/了解/讨论了 xxx。首先 yyy，其次 zzz，接着 aaa，然后bbb，最后ccc。

（同时把`summary`以表格方式总结）

本章我们一起学习了以下概念：

| 概念 | 解释 |
|------|------|
| XXX | 一句话总结 |

（最后添加一段话的下一章预告）

下一章中，我们将学习 xxx 。
[/content]

## 示例片段

**类型定义讲解**：

[content]
### 2.1 类型定义详解

#### Store 接口
```typescript
export type Store<T> = {
  // 读：获取当前状态
  getState: () => T
  // 写：基于旧状态计算新状态
  setState: (updater: (prev: T) => T) => void
  // 订阅：状态变更通知
  subscribe: (listener: Listener) => () => void  
}
```

这三个方法构成了状态管理的"最小完备集合"：**getState**授予了读权限，**setState**授予了写权限（函数式更新保证原子性），**subscribe**授予了监听权限（发布-订阅模式）。
[/content]

**逐行代码解析**：

[content]
### 2.2 setState 核心逻辑

```typescript
setState: (updater: (prev: T) => T) => {
  // ① 保存旧状态（用于后续比较）
  const prev = state
  // ② 执行更新函数，计算新状态           
  const next = updater(prev)
  // ③ 没变？短路退出（性能优化关键）      
  if (Object.is(next, prev)) return
  // ④ 更新闭包中的状态  
  state = next
  // ⑤ 触发副作用回调                 
  onChange?.({ newState: next, oldState: prev })
  // ⑥ 通知所有订阅者
  for (const listener of listeners) listener()    
}
```

**为什么用 Object.is 而不是 ===？**

`Object.is(NaN, NaN)` 返回 true（=== 返回 false），而`Object.is(+0, -0)` 返回 false（=== 返回 true），提供了更精确的状态变化检测。
[/content]

**设计取舍分析**：

[content]
### 为什么不用 middleware？

Redux 使用 middleware 链处理副作用，但 Claude Code 选择了更简单的方案：

Middleware链复杂度高，适用于多种副作用类型需要插拔的场景，而单一onChange回调复杂度低，适用于副作用类型有限且固定的场景。

Claude Code 的副作用类型只有（1）写配置文件，（2）通知远端和（3）清缓存。一个 `onChange` 回调足够应对，34行代码不值得引入 middleware 的复杂度。
[/content]

## 细纲

```
{detail}
```

## 对应源码

[content]
{code}
[/content]
'''